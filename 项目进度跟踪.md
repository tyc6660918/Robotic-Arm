# Robotic-Arm（低成本外科手术机器人训练平台）

## 1. 区域功能定位

本区域实现低成本（<6000 RMB）的主从遥操作外科手术机器人训练平台。系统集成如下模块：
- **Dummy 六自由度机械臂**（基于 STM32F405RG 的从动端，搭载谐波减速器）
- **RST 绳驱动外科手术夹爪**（基于 STM32F103ZET6 的三自由度末端执行器，采用 TB6612 驱动器）
- **Windows 仿真环境**（基于 Python 的离线数字孪生，内置数值逆运动学求解器）
- **U-Arm 主端设备**（计划中的遥操作输入端，Config1 设计已完成）

目标性能指标：端到端延迟 <100ms，定位精度达亚毫米级，用于医学训练与技能评估。

## 2. 关键文件

| 文件路径 | 用途 | 状态 | 备注 |
|-----------|---------|--------|-------|
| `E:\Robotic-Arm\项目完整技术文档.txt` | 主技术规格书，涵盖项目愿景、市场分析、系统架构、硬件规格与开发里程碑 | 完整且详细（570 行） | 记录外科手术机器人训练平台愿景、硬件规格（Dummy 机械臂运动学：L_BASE=0.109m、L_ARM=0.146m 等）、RST 夹爪参数（GMR 编码器 2400 线/转、0.15° 分辨率）及风险缓解策略 |
| `E:\Robotic-Arm\RST夹取端控制设计方案.txt` | RST 夹爪详细控制设计（三电机直流控制，采用 TB6612 驱动器） | 完整实现规范 | 规定引脚分配、GMR 编码器配置、PID 控制回路（位置 1kHz、速度 5kHz、电流限制 20kHz）及校准流程 |
| `E:\Robotic-Arm\robots\Dummy-Arm\firmware\stm32-control\control\Core\Inc\rst_config.h` | 固件硬件抽象与配置 | 已实现 | 与设计规范匹配 — TIM1/8 PWM、TIM2/3/4 编码器、ADC1 电流采样、通过 TIM6 实现 20kHz 控制回路 |
| `E:\Robotic-Arm\robots\Dummy-Arm\firmware\stm32-control\control\Core\Src\control_loop.c` | 实时 20kHz 控制实现 | 已实现，含编码器读取、PID、电流限制 | 实现编码器溢出处理、速度估计与状态机 |
| `E:\Robotic-Arm\tools\windows_sim\sim\ik_solver.py` | 带热启动的有界数值逆运动学求解器 | 生产就绪，含回退策略 | 基于 SciPy 最小二乘法，可配置公差（位置 1e-4m、姿态 0.05°），多尝试引导启动 |
| `E:\Robotic-Arm\tools\windows_sim\sim\kinematics.py` | 正运动学与雅可比矩阵计算 | 已实现并通过验证测试 | 齐次变换、SO(3) 右雅可比、基于 URDF 的运动链构建 |
| `E:\Robotic-Arm\tools\windows_sim\runs\20260808_031244\report.json` | 仿真验证指标 | 代表性运行结果，展示系统性能 | IK 成功率 1.0，位置 RMS 误差 0.146mm，姿态 RMS 误差 0.304mrad，100Hz 控制频率下 117 个采样点 |
| `E:\Robotic-Arm\docs\technical\architecture.md` | 涵盖所有子系统的系统架构文档 | 全面的多区域分析 | 记录 windows_sim（离线运动学验证）、robots（硬件/固件）、tools（CLI/查看器）及集成点 |
| `E:\Robotic-Arm\robots\Dummy-Arm\firmware\stm32-control\00-安全第一.md` | 硬件安全与连接指南 | 完整的初学者友好指南 | 三阶段测试方法（仅 MCU → 编码器 → 电机），含安全检查清单以防止硬件损坏 |
| `E:\Robotic-Arm\robots\Dummy-Arm\firmware\stm32-control\01-傻瓜式操作手册.md` | CubeMX 配置与固件设置的分步指南 | 详细教程格式 | 弥补 DeepSeek 配置缺口 — TIM6 与 ADC 设置附精确参数值 |

## 3. 当前进度

| 组件 | 状态 | 证据 | 备注 |
|-----------|--------|----------|-------|
| 硬件文档 | 完成 | REF 板、42/35 驱动板，含详细原理图与引脚分配，位于 `robots/Dummy-Arm/hardware/` | 已具备可用于生产的 Gerber 文件与 BOM |
| RST 夹爪控制设计 | 完成 | `RST夹取端控制设计方案.txt`（完整规范） | 三电机独立控制，采用 TB6612 驱动器，规格已完整定义 |
| RST 夹爪固件 | **未完成（0%）** | 设计完成但交接确认尚无任何实现 | 关键缺口：无 PID 回路、无协议处理器、无安全功能 |
| Dummy-Arm 固件 | 完成 | `robots/Dummy-Arm/firmware/stm32-control/` 含 20kHz 控制回路、PID、编码器读取 | STM32F103ZET6 实现与 `rst_config.h` 规范匹配 |
| Windows 仿真 | 生产就绪 | 7 次以上成功运行，`tools/windows_sim/runs/` 中 IK 成功率 100% | 亚毫米级精度：位置 RMS 0.146mm，姿态 RMS 0.304mrad |
| IK 求解器 | 生产就绪 | `tools/windows_sim/sim/ik_solver.py` 含有界优化与热启动 | 验证显示 1000 步跟踪下 P99 计算时间 < 20ms |
| 测试基础设施 | 完成 | IK 单元测试、几何雅可比验证、遥操作性能指标 | 交接分析显示 92 项测试通过 |
| 项目文档 | 完成 | `项目完整技术文档.txt` 含市场分析、系统架构、风险评估 | 570 行，涵盖从愿景到 M7 里程碑的完整内容 |
| 安全文档 | 完成 | `00-安全第一.md` 硬件连接指南，含分步启动流程 | 初学者友好的三阶段测试方法 |
| 调试环境 | 完成 | VSCode 配置、OpenOCD 支持（ST-Link/CMSIS-DAP/WCH-Link） | 8 项调试配置，已启用 SWO/ITM 追踪 |
| URDF 机器人模型 | 完成 | `robots/Dummy-Arm/urdf/dummy.urdf` 用于 ROS2/MoveIt2 集成 | 路径不一致：仿真引用旧的 'Moveit_ws-main' 路径 |
| RST 机械装配 | **未完成** | 记录为 M4 里程碑 — 待进行 | 无物理夹爪装配或安装验证的证据 |
| 主从遥操作 | **未完成** | 记录为 M6 里程碑 | U-Arm 与 Dummy-Arm 集成尚未实现 |
| 端到端延迟测试 | **未启动** | 已指定 <100ms 目标但尚未测量 | 实际硬件验证受固件未完成与 U-Arm 未装配的阻塞 |
| 训练平台功能 | **未启动** | M7 里程碑：数据记录/回放、技能评估系统 | 除高层目标外，需求尚未记录 |
| BOM 成本验证 | **未完成** | 已指定 <6000 RMB 目标但最终成本未确认 | 谐波减速器采购（国产目标）需供应商验证 |
| 运动学参数一致性 | **需验证** | DH 参数已记录但未在固件/仿真/URDF 间交叉验证 | 仿真使用的 URDF 路径与仓库结构不匹配 |

## 4. 已完成功能

- 硬件文档（REF 板、42/35 驱动板，含详细原理图与引脚分配）
- RST 夹爪控制设计规范（三电机独立控制，采用 TB6612 驱动器）
- 固件实现（STM32F103ZET6，含 20kHz 控制回路、PID、编码器读取、ADC 电流采样）
- Windows 仿真环境（IK 求解器、运动学、运动滤波、硬件在环能力）
- 测试基础设施（IK 单元测试、几何雅可比验证、遥操作性能指标）
- 项目文档（全面技术文档，含市场分析、系统架构、风险评估）
- 安全文档（硬件连接指南、分步启动流程）
- 调试环境（VSCode 配置、OpenOCD 支持、多种调试器选项）
- URDF 机器人模型（用于 ROS2/MoveIt2 集成）
- 仿真验证运行（7 次以上成功运行，IK 成功率 100%，亚毫米级定位精度）

## 5. 未完成内容

| 任务 | 优先级 | 阻塞因素 | 下一步 |
|------|----------|---------|-----------|
| RST 夹爪固件实现 | **关键（P0）** | 交接确认尽管设计规范完整但尚无任何实现。无 PID 回路、无串行协议（P<>U<>L<>）、无安全功能（电流限制、失速检测、看门狗）。估计实现缺口 75%。 | 按 `RST夹取端控制设计方案.txt` 实现多速率控制（位置 1kHz、速度 5kHz、电流 20kHz）、GMR 编码器读取（2400 线/转）、TB6612 PWM 驱动集成与 ASCII 协议处理器 |
| RST 机械装配与物理夹爪验证 | **高** | 记录为 M4 里程碑 — 待进行。无机械装配、Dummy 臂法兰安装或线缆布线/张紧的证据。 | 采购 OpenRST CAD 零部件（47MB 压缩包可用），组装驱动单元 + 末端执行器，安装至 Dummy 法兰，对照仿真预测进行验证 |
| 主从遥操作集成 | **高** | U-Arm 与 Dummy 集成尚未实现（M6 里程碑）。主端与 PC 间的通信协议未建立。实时延迟未测量。 | 装配 U-Arm Config1 硬件（BOM 约 $60，估计 1-2 周），实现 ServoReader→IK→Dummy 命令链路，对照 <100ms 目标测量端到端延迟 |
| 端到端延迟测试 | **高** | 已指定 <100ms 目标但尚未测量。实际硬件验证受固件未完成与 U-Arm 未装配的阻塞。 | 完成 RST 固件与 U-Arm 硬件后，测量完整遥操作链路：伺服读取 → 串行 → PC IK（已知 P99 20ms）→ 串行 → Dummy 执行 |
| 运动学参数一致性验证 | **中** | DH 参数记录于 `项目完整技术文档.txt` 但未在固件、仿真（`kinematics.py`）与 URDF（`dummy.urdf`）间交叉验证。 | 从全部三个来源提取参数，创建对比表，解决差异，建立单一可信数据源 |
| URDF 路径引用更新 | **中** | 仿真代码引用不存在的 'Moveit_ws-main'，但仓库实际使用 'moveit2_ws'。导致 `urdf_model.py` 中 URDF 加载失败。 | 在 `tools/windows_sim/` 中查找替换 'Moveit_ws-main' → 'moveit2_ws'，更新文档引用 'rst-control-fw' → 'robots/Dummy-Arm/firmware/stm32-control' |
| 固件-仿真参数对齐 | **中** | DH 参数与控制回路时序分别记录。无集成测试验证固件行为与仿真匹配。 | 创建参数验证脚本，添加固件 FK 实现，验证关节限位与运动学是否与仿真匹配 |
| 训练平台功能 | **低** | M7 里程碑未启动。数据记录/回放系统、技能评估指标、GEARS 兼容评估框架均缺失。 | 实现前先定义功能需求、评估指标与临床验证计划 |
| BOM 成本验证 | **低** | 已指定 <6000 RMB 目标但最终成本未确认。谐波减速器采购（国产目标）需价格验证。 | 联系供应商获取谐波减速器、电机、编码器与电子元件报价，执行成本审计 |
| 生产就绪硬件验证 | **低** | 作为未来目标提及但尚未完成。无长期可靠性测试或累积漂移特性分析。 | M6 遥操作里程碑达成后，运行扩展遥操作会话（1 小时以上）以表征漂移与热行为 |

## 6. 使用说明

### RST 夹爪控制（STM32F103ZET6）

**硬件配置：**
- 三自由度绳驱动外科手术工具（俯仰 + 双钳口）
- MG310L 直流电机，配 GMR 编码器（2400 线/转、0.15° 分辨率、50:1 减速比）
- TB6612FNG H 桥驱动器（双通道配置）
- INA180A1 电流采样（0.1Ω 分流电阻、20 倍增益、约 4mA/位 分辨率）
- 控制回路架构：
  - 电流限制回路：20kHz（TIM6 中断）
  - 速度回路：5kHz
  - 位置回路：1kHz

**引脚分配（参照 `rst_config.h`）：**
- TIM1/TIM8：电机驱动器 PWM 生成
- TIM2/TIM3/TIM4：编码器输入（正交模式）
- ADC1：电流采样（3 通道）
- UART：主机通信（115200 波特率、8N1）

**固件状态：** 设计完成，实现度 0%。关键缺口阻塞所有硬件验证。

### Dummy 六自由度机械臂运动学

**DH 参数：**
- L_BASE = 109mm（基座高度）
- D_BASE = 35mm（肩部偏移）
- L_ARM = 146mm（上臂）
- L_FOREARM = 115mm（前臂）
- D_ELBOW = 52mm（肘部偏移）
- L_WRIST = 72mm（腕部至法兰）

**硬件：**
- 谐波减速器（国产目标，用于成本控制）
- 42/35 步进电机，配 MT6816 磁编码器
- CAN 总线 1Mbps + UART 115200bps 双协议控制

**控制接口：**
- 关节空间：带目标位置的 MoveJ 命令
- 笛卡尔空间：MoveL 需 PC 端 IK 计算
- 实时回路：200Hz 控制频率（基于 FreeRTOS）

### Windows 仿真环境

**运行仿真：**
```bash
cd E:\Robotic-Arm\tools\windows_sim
python run_sim.py --scenario <scenario_file>
```

详细使用说明请参阅：[tools/windows_sim/Windows离线仿真说明.md](tools/windows_sim/Windows离线仿真说明.md)

**IK 求解器配置：**
- 算法：带边界约束的 SciPy 最小二乘法
- 公差：位置 1e-4m（0.1mm）、姿态 0.05°（0.87mrad）
- 热启动跟踪，含 12 个回退引导位置
- 性能：1000 步跟踪下 P99 < 20ms

**验证结果（源自 `runs/20260808_031244/report.json`）：**
- IK 成功率：100%（117 个采样点）
- 位置 RMS 误差：0.146mm
- 姿态 RMS 误差：0.304mrad
- 控制频率：100Hz

**硬件在环模式：**
`MockJointPlant` 模拟物理动力学（惯量、摩擦、执行器限位），用于固件-仿真协同验证。

### 安全规程

**参照 `00-安全第一.md` 三阶段测试方法：**

1. **阶段 1：仅 MCU** — 连接外设前验证电源、时钟、调试接口
2. **阶段 2：编码器** — 用示波器验证编码器信号，确认正交时序
3. **阶段 3：电机** — 限流测试（0.3A 校准模式），全功率供电前确认转向

**关键安全检查：**
- GMR 编码器信号质量验证（风险 R2 已标记）
- RST 夹爪线缆拉伸监测（风险 R1 已记录）
- 固件中强制执行电流限制（正常 1.0A / 校准 0.3A）
- 失速检测与看门狗（设计已指定，实现待进行）

### 调试环境

**支持的调试探针：**
- ST-Link v2/v3
- CMSIS-DAP 兼容适配器
- WCH-Link（用于 CH32 与 STM32）

**VSCode 配置：**
`.vscode/launch.json` 中提供 8 项调试配置，已启用 SWO/ITM 追踪。

详细配置说明请参阅：
- [debug/VSCode调试配置说明.md](debug/VSCode调试配置说明.md)
- [debug/OpenOCD调试说明.md](debug/OpenOCD调试说明.md)

**OpenOCD 自动检测：**
```bash
cd E:\Robotic-Arm\openocd-configs
./test-connection.sh
```

自动检测探针类型与目标 MCU（F103/F405）。

### ROS2 MoveIt2 集成

**工作空间：** `E:\Robotic-Arm\moveit2_ws`

**注意：** colcon 未安装 — 工作空间构建受阻。安装 colcon 后执行：
```bash
cd E:\Robotic-Arm\moveit2_ws
colcon build --symlink-install
source install/setup.bash
ros2 launch dummy_moveit_config demo.launch.py
```

**动作服务器：** `dummy_arm_controller.py` 实现 `FollowJointTrajectory` 用于 MoveIt2 集成。

**坐标变换：** 按 URDF 定义，J3 偏移 +90°，J5/J6 反转。

## 7. 风险与限制

| 风险 | 影响程度 | 证据 | 缓解措施 |
|------|--------|----------|------------|
| 路径不一致导致 URDF 加载失败 | 中 | 仿真代码引用 'Moveit_ws-main' 但仓库使用 'moveit2_ws'。多份文档引用旧的 'rst-control-fw' 路径。 | 在 `tools/windows_sim/` 与文档中执行查找替换操作。低投入、高影响的修复，可显著改善开发者上手体验。 |
| 各子系统间运动学参数漂移 | 中-高 | DH 参数记录于 `项目完整技术文档.txt`，但未集中对照固件、仿真（`kinematics.py`）与 URDF（`dummy.urdf`）进行验证。 | 创建参数对比表，建立单一可信数据源（推荐 URDF），添加验证测试。 |
| 主从延迟可能超过 100ms 目标 | 高 | 已指定 <100ms 目标但从未测量。U-Arm 通过串行以 50-100Hz 读取伺服，PC 计算 IK（已验证 P99 20ms），通过串行向 Dummy 发送命令。Windows_sim 时间预算显示每周期 4-7ms，但尚无端到端硬件测试。 | U-Arm 装配后测量实际延迟，分析通信瓶颈。如串行性能受限，考虑 USB 批量传输或直接 CAN 接口。 |
| 谐波减速器成本超支 | 中 | 目标 <6000 RMB，采用国产谐波减速器，但最终成本未验证。OpenRST 电机替换分析显示 Maxon RE10 成本 ¥3000-5000 对比 WHEELTEC ¥100-300，表明 Dummy 减速器面临类似成本压力。 | 联系供应商获取报价，执行成本审计。若谐波减速器超出预算，记录备用方案（带传动、行星齿轮箱）。 |
| RST 夹爪线缆拉伸与滞后 | 中 | 已作为 R1 风险记录并提出缓解措施，但无量化补偿策略。Windows_sim `IdealOpenRSTModel` 有意忽略线缆动力学与摩擦。 | 在仿真中添加线缆拉伸模型，实施校准流程以测量滞后，在固件中设计前馈补偿。 |
| GMR 编码器信号质量 | 中 | 已作为 R2 风险记录。需示波器验证与噪声滤波。 | 遵循 `00-安全第一.md` 阶段 2 测试。电机通电前验证正交信号。若检测到噪声，考虑硬件 RC 滤波或固件去抖。 |
| 训练平台临床有效性 | 低-中 | 技能评估需临床验证以确保手术相关性（M7 里程碑）。除高层 GEARS 参考外，尚无评估框架。 | 实现数据记录功能前，与医学专业人员合作制定验证协议。 |
| 文档分散导致缺乏单一可信数据源 | 中 | 关键规格分散于 `项目完整技术文档.txt`、`RST夹取端控制设计方案.txt`、代码注释与 URDF 中。无集中参数注册表。 | 创建 `parameters.yaml`，收录所有 DH 参数、电机规格、编码器分辨率、控制回路频率，供所有子系统引用。 |

## 8. 依赖关系

**构建工具与编译器：**
- STM32CubeMX，用于外设初始化代码生成
- Keil MDK-ARM 或 arm-none-eabi-gcc，用于固件编译（已安装 ARM GCC 12.2）
- CMake 4.4.2，用于构建系统管理

**调试与烧录：**
- OpenOCD 0.12.0-7 + GDB，用于调试（支持 CMSIS-DAP/ST-Link/WCH-Link）
- SWD 调试器硬件（ST-Link v2/v3、CMSIS-DAP 或 WCH-Link）

**Python 仿真环境：**
- Python 3.13+（已安装 3.13.7）
- numpy 2.4.3（数值计算）
- scipy（最小二乘 IK 求解器）
- matplotlib（`robot_viewer.py` 中 15Hz 3D 机器人可视化）
- pyserial（主机-固件通信）

**ROS2 集成（可选）：**
- ROS2 Humble + MoveIt2，用于运动规划集成
- colcon 构建工具（**未安装 — 阻塞工作空间构建**）
- URDF 文件路径解析（仿真引用旧路径 'Moveit_ws-main'，实际路径为 'moveit2_ws'）

**硬件组件：**
- TB6612FNG 电机驱动板（RST 夹爪双 H 桥）
- GMR 磁编码器（12 线、4x 正交、50:1 减速 → 2400 线/转）
- 谐波减速器（Dummy 臂国产目标 — 采购需确认）
- MT6816 磁编码器（Dummy 臂步进电机）
- INA180A1 电流采样放大器（20 倍增益、0.1Ω 分流电阻）
- CH340 USB-UART 适配器，用于调试

**CAD 与机械：**
- OpenRST CAD 压缩包（47MB，含 STL/STEP/SolidWorks 格式，CERN-OHL-S v2 许可证）
- 3D 打印机，用于 U-Arm Config1 零部件（11 个 STL 文件，估计材料成本约 $60）

## 9. 下一步计划

**优先级 0（关键路径）：**

1. **修复 STM32-control 固件初始化以启用 RST 夹爪控制回路**
   - 理由：根据交接分析，STM32F103ZET6 固件仅完成 25%。TIM6 20kHz 控制回路从未初始化、GPIO 时钟未启用、PWM/编码器通道未启动、ADC 电流采样未完成。无可用固件，所有 RST 夹爪硬件验证均被阻塞。
   - 操作：在 `main.c` 中添加 `TIM6_Init()` 调用，启用 GPIO 时钟，启动 PWM/编码器通道，按 `01-傻瓜式操作手册.md` 完成 ADC 配置。
   - 验证：用示波器在测试引脚验证 20kHz 中断频率，手动旋转电机确认编码器读取，连接电机前验证 PWM 输出。

**优先级 1（快速收益）：**

2. **更新仿真代码中的 URDF 路径并修复文档路径引用**
   - 理由：仿真引用不存在的 'Moveit_ws-main' 导致 URDF 加载失败（实际为 'moveit2_ws'）。多份文档引用已删除的 'rst-control-fw' 路径。低投入、高影响的修复，可解除验证工作流阻塞。
   - 操作：在 `tools/windows_sim/` 中查找替换 'Moveit_ws-main' → 'moveit2_ws'，在文档中更新 'rst-control-fw' → 'robots/Dummy-Arm/firmware/stm32-control'。
   - 验证：运行 `python run_sim.py` 并确认 URDF 无错误加载。

3. **安装 colcon 并构建 ROS2 工作空间以启用 MoveIt2 运动规划**
   - 理由：colcon 是标准 ROS2 构建工具，可通过 pip 轻松安装。构建 moveit2_ws 可解锁 `FollowJointTrajectory` 动作服务器与 MoveIt2 运动规划栈。
   - 操作：`pip install colcon-common-extensions`，然后 `cd moveit2_ws && colcon build --symlink-install`。
   - 验证：使用 `ros2 launch dummy_moveit_config demo.launch.py` 启动演示，确认 RViz 可视化 Dummy 机械臂。

**优先级 2（硬件集成）：**

4. **交叉验证文档、仿真与 URDF 间的运动学参数**
   - 理由：DH 参数在多处记录但缺乏集中验证。参数漂移可能导致仿真-硬件不匹配。
   - 操作：创建对比表，从 `项目完整技术文档.txt`、`kinematics.py` 与 `dummy.urdf` 中提取 L_BASE、L_ARM、L_FOREARM、L_WRIST、D_ELBOW。解决差异。
   - 验证：添加单元测试，验证各实现的 FK 输出匹配。

5. **采购并装配 U-Arm Config1 硬件以启用主从测试**
   - 理由：Config1 设计完成（`config1.STEP`、11 个 STL 零部件）但需物理硬件用于伺服校准、URDF 验证与遥操作延迟测量。阻塞 M6 里程碑。
   - 操作：订购 BOM 组件（估计约 $60），3D 打印零部件，按设计装配。估计 1-2 周。
   - 验证：给伺服上电，使用 `ServoReader` 类验证串行通信，校准零点。

6. **实现 RST 夹爪固件：串行协议处理器、PID 控制回路、安全功能**
   - 理由：最大的剩余固件缺口。交接显示实现缺口约 75%。设计指定 P<>U<>L<> ASCII 协议、三电机独立控制、级联 PID（位置/速度/电流）、电流限制（正常 1.0A / 校准 0.3A）、失速检测。
   - 操作：实现多速率控制结构（20kHz 电流、5kHz 速度、1kHz 位置）、带溢出处理的 GMR 编码器读取、TB6612 PWM 生成、ASCII 协议解析器、安全状态机。
   - 验证：连接电机前分别对每个控制回路进行单元测试，用台式电源验证电流限制，用堵转电机验证失速检测。

7. **执行端到端主从遥操作集成与延迟测量**
   - 理由：M6 里程碑要求完整的 U-Arm → PC → Dummy → RST 链路验证。<100ms 延迟目标需对照实际硬件测量。
   - 操作：RST 固件完成且 U-Arm 装配后，实现 `ServoReader → IK → Dummy 命令` 流水线。用时间戳分析每个阶段。
   - 验证：测量端到端延迟（伺服读取至 Dummy 运动开始），确认 <100ms 目标，表征抖动与最坏情况延迟。

**优先级 3（未来工作）：**

8. **添加固件运动学模型并验证与仿真的一致性**
   - 理由：固件仅实现关节空间控制。嵌入式 FK 可在 PC 链路失败时启用笛卡尔控制回退。
   - 操作：将正运动学从 `kinematics.py` 移植到 C，添加至固件，通过串行命令暴露。

9. **开发训练平台功能：数据记录/回放、技能指标、GEARS 兼容评估**
   - 理由：M7 里程碑。实现前需先定义功能需求。
   - 操作：与医学专业人员合作定义评估指标，然后实现数据记录基础设施。

10. **执行 BOM 成本验证以确认 <6000 RMB 目标**
    - 理由：谐波减速器采购（国产目标）需供应商价格验证以避免成本超支。
    - 操作：向供应商请求报价，若超出目标则记录备用方案。

---

相关文档链接：
- 项目总览与构建系统说明：[项目总览与构建系统说明.md](项目总览与构建系统说明.md)
- 脚本使用说明：[脚本使用说明.md](脚本使用说明.md)
- 调试配置说明：[debug/VSCode调试配置说明.md](debug/VSCode调试配置说明.md)、[debug/OpenOCD调试说明.md](debug/OpenOCD调试说明.md)
- 工具说明：[tools/simple-cli/命令行控制工具说明.md](tools/simple-cli/命令行控制工具说明.md)、[tools/windows_sim/Windows离线仿真说明.md](tools/windows_sim/Windows离线仿真说明.md)
