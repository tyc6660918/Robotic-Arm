# 命令行控制工具说明

- 电机 PID 参数微调命令行工具
1. 将 `sername = "/dev/tty.usbmodem204C397F32321"` 修改为您的串口设备名，可通过 dmesg 命令查找
1. 运行 `'python3 simple_cli.py'` 启动程序
1. 输入 `'start'` 命令启动串口并使能电机
1. 输入 `'kp 1 200'`，将电机 1 的 P 参数修改为 200
1. 输入 `'reboot 1'`，重启电机 1 以启用新参数
1. 按下电机 1 的按键以使用新参数重新进行标定
1. 输入 `'stop'` 将禁用电机
