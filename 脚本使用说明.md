# 根目录脚本与实用工具

## 1. 区域功能定位

本区域涵盖仓库中所有实用脚本、工具与自动化功能。不同于典型单仓库在根目录放置安装脚本的做法，本项目将脚本分发至各功能目录：`debug/` 中的硬件调试工具、`tools/` 中的 Python 控制实用程序，以及各固件子目录中的构建自动化。该工具集支持三种主要工作流：（1）硬件启动与 OpenOCD 调试；（2）基于串行的机器人控制与可视化；（3）用于运动学验证的离线仿真。

## 2. 关键文件

| 文件路径 | 用途 | 状态 | 备注 |
|-----------|---------|--------|-------|
| `E:\Robotic-Arm\debug\openocd-configs\test-connection.sh` | 硬件连接验证 — 测试所有调试器类型并报告可用配置 | 有效 | Bash 脚本，检查 CP2102、CMSIS-DAP、ST-Link、WCH-Link。成功时输出使用示例。 |
| `E:\Robotic-Arm\tools\simple-cli\dummy_port_scanner.py` | 智能端口检测，使用 USB VID/PID 与 `#GETJPOS` 探测 | 有效 | 独立工具。用法：`python dummy_port_scanner.py [--connect]`。目标设备：CP2102（10C4:EA60）与 STM32 CDC（0483:5740）。 |
| `E:\Robotic-Arm\tools\simple-cli\simple-cli.py` | Dummy 机器人交互式串行 CLI（v4，协议对齐，调试模式） | 有效 | 命令：enable、disable、move、movej、jpos、lpos、home、kp/ki/kd 参数整定、原始命令。自动检测 COM5/COM6。 |
| `E:\Robotic-Arm\tools\simple-cli\streaming_control.py` | 50Hz 实时键盘控制，双模式（关节/笛卡尔） | 有效 | 911 行。聚焦范围输入、有界串行写入。TAB 切换模式，SPACE 冻结，1-9 设置速度。需 Windows 系统。 |
| `E:\Robotic-Arm\tools\robot-viewer\robot_viewer.py` | 实时 3D 可视化，采用粗管连杆与串行反馈 | 有效 | 320 行。使用 matplotlib 3D + 线程。支持 `--no-connect` 进行离线演示。查询频率 15Hz。 |
| `E:\Robotic-Arm\tools\windows_sim\run_sim.py` | 离线遥操作仿真器，含 OpenRST、频谱分析、场景回放 | 有效 | 1237 行。完整被控对象模型、One Euro 滤波器、IK 求解器、安全状态机。支持无头模式与 GUI 模式。 |
| `E:\Robotic-Arm\tools\windows_sim\validate_kinematics.py` | 确定性 FK→IK→FK 验收验证 | 有效 | 167 行。生成 1000 个随机位姿，求解 IK，验证位置 <0.1mm 且姿态 <0.05°。 |
| `E:\Robotic-Arm\robots\Dummy-Arm\firmware\stm32-control\control\MDK-ARM\build.bat` | Keil uVision 无头构建自动化 | 有效 | 调用 UV4.exe -b，输出构建日志，报告错误/警告。硬编码路径 `E:\KEIL\UV4`。 |
| `E:\Robotic-Arm\tools\simple-cli\test_streaming_control.py` | streaming_control.py 串行协议与键盘处理的单元测试 | 有效 | 513 行。测试 FIFO 跟踪、泵送逻辑、写入超时、聚焦范围输入。FakeController 模拟固件行为。 |

## 3. 当前进度

| 组件 | 状态 | 证据 | 备注 |
|-----------|--------|----------|-------|
| OpenOCD 硬件检测脚本 | 完成 | `test-connection.sh`（138 行）含 CP2102/CMSIS-DAP/ST-Link/WCH-Link 检测 | 自动检测调试器类型并输出使用示例 |
| 基于 VID/PID 的端口扫描器 | 完成 | `dummy_port_scanner.py` 目标 CP2102（10C4:EA60）与 STM32 CDC（0483:5740），含协议验证 | 带 `--connect` 选项的独立工具 |
| 交互式串行 CLI | 完成 | `simple-cli.py` 含调试模式、PID 参数整定（kp/ki/kd）、原始命令透传 | 自动检测 COM5/COM6，协议 v4 对齐 |
| 50Hz 键盘点动工具 | 完成 | `streaming_control.py`（911 行）含 `test_streaming_control.py`（513 行）中的 18 项单元测试 | 聚焦范围输入、双模式（关节/笛卡尔）、仅 Windows |
| 3D matplotlib 可视化 | 完成 | `robot_viewer.py`（320 行）含粗管连杆、15Hz 实时串行数据 | 支持 `--no-connect` 进行离线演示 |
| 离线遥操作仿真器 | 完成 | `run_sim.py`（1237 行）含 One Euro 滤波器、IK 求解器、安全状态机、场景回放 | 交接分析中唯一生产就绪模块 |
| FK→IK→FK 验证套件 | 完成 | `validate_kinematics.py` 在 1000 个随机位姿下成功率达 99% 以上 | 位置公差 0.1mm，姿态公差 0.05° |
| Keil 无头构建脚本 | 完成 | `build.bat` 含 UV4.exe 自动化、错误/警告报告 | 硬编码路径 `E:\KEIL\UV4` 限制可移植性 |
| 流式控制单元测试 | 完成 | `test_streaming_control.py` 中的 18 项测试，涵盖 FIFO、泵送逻辑、超时、聚焦范围输入 | FakeController 模拟验证串行协议 |
| 根目录安装脚本 | 缺失 | 仓库根目录未发现 `setup.sh`、`install.bat` 或 `requirements.txt` | 用户必须手动安装依赖 |
| 跨平台键盘控制 | 未完成 | `streaming_control.py` 使用 `ctypes.windll`（仅 Windows） | 未实现 Linux/macOS 替代方案 |
| 统一 CLI 封装 | 缺失 | Python 工具分散于 `tools/simple-cli`、`tools/robot-viewer`、`tools/windows_sim` | 无带子命令的单一入口点 |
| 依赖文档 | 缺失 | Python 工具无 `requirements.txt` 或依赖安装指南 | 需从 import 语句手动提取 |

## 4. 已完成功能

- 定位并读取根目录与子目录中的所有脚本
- 分析 `test-connection.sh` OpenOCD 硬件检测脚本，支持 4 种调试器类型
- 记录 `dummy_port_scanner.py` 基于 VID/PID 的端口发现与协议验证
- 分析 `simple-cli.py` 交互式控制工具，含调试模式与 PID 参数整定
- 记录 `streaming_control.py` 50Hz 键盘点动（911 行）与 18 项单元测试
- 分析 `robot_viewer.py` 3D 可视化，使用 matplotlib，查询频率 15Hz
- 记录 `run_sim.py` 遥操作仿真器（1237 行），含完整控制回路与场景回放
- 分析 `validate_kinematics.py` 验收测试，FK→IK→FK 成功率达 99% 以上
- 记录 `build.bat` Keil 自动化脚本，含错误/警告报告
- 分析 `test_streaming_control.py` 单元测试（513 行），含 FakeController 模拟
- 审查 `fk_solver.py` 基于 URDF 的正运动学
- 检查根目录安装脚本（未发现）
- 识别跨 28 个项目的基于 CMake 的构建系统结构

## 5. 未完成内容

| 任务 | 优先级 | 阻塞因素 | 下一步 |
|------|----------|---------|-----------|
| 根目录设置脚本 | 中 | 新开发者无自动化依赖安装 | 创建 `setup.sh`/`setup.bat`，含 Python 依赖安装与 OpenOCD 路径验证 |
| Python 依赖清单 | 中 | 无 `requirements.txt` — 用户必须手动安装 pyserial、numpy、scipy、matplotlib、PyQt5、ursina | 从工作环境提取版本，生成固定版本的 `requirements.txt` |
| 跨平台键盘控制 | 低 | `streaming_control.py` 使用 Windows 专用的 `ctypes.windll.winmm`、`kernel32`、`user32` | 研究 `pynput` 或 `keyboard` 库以实现 Linux/macOS 兼容 |
| 统一 CLI 入口点 | 低 | 工具分散，要求用户浏览多个目录并记住脚本名称 | 创建 `tools/robot_cli.py`，带子命令：`port-scan`、`control`、`stream`、`view`、`sim`、`validate` |
| 可移植构建脚本 | 低 | `build.bat` 硬编码 `E:\KEIL\UV4` — 在其他安装中失败 | 使用环境变量 `%KEIL_PATH%` 或注册表查询 Keil 安装路径 |
| 可移植 OpenOCD 路径 | 低 | `test-connection.sh` 硬编码 `/c/Users/TYC/.embedded-tools/openocd/...` | 使用 `which openocd` 或检查标准 xPack 安装路径 |
| 工具专用 README 文件 | 低 | 使用说明分散于源代码注释 — 无独立文档 | 在 `tools/simple-cli/`、`tools/robot-viewer/`、`tools/windows_sim/` 中创建 README.md |
| CI 安全构建脚本 | 低 | `build.bat` 使用 `pause` 命令，在非交互环境中挂起 | 替换为退出码传递，用于自动化构建系统 |

## 6. 使用说明

### Hardware Debugging

**OpenOCD 连接测试：**
```bash
cd debug/openocd-configs
bash test-connection.sh
```
自动检测 CP2102、CMSIS-DAP、ST-Link、WCH-Link 调试器，并报告可用配置及使用示例。

详细调试说明请参阅：
- [debug/OpenOCD调试说明.md](debug/OpenOCD调试说明.md)
- [debug/VSCode调试配置说明.md](debug/VSCode调试配置说明.md)

### Serial Control Tools

**端口检测：**
```bash
cd tools/simple-cli
python dummy_port_scanner.py [--connect]
```
扫描 CP2102（VID:PID 10C4:EA60）与 STM32 CDC（0483:5740），使用 `#GETJPOS` 协议探测进行验证。

**交互式 CLI：**
```bash
cd tools/simple-cli
python simple-cli.py
```
命令：`enable`、`disable`、`move`、`movej`、`jpos`、`lpos`、`home`、`kp <val>`、`ki <val>`、`kd <val>` 及原始命令。自动检测 COM5/COM6。

**50Hz 键盘点动：**
```bash
cd tools/simple-cli
python streaming_control.py
```
控制：TAB（切换关节/笛卡尔模式）、SPACE（冻结）、1-9（设置速度）、Q/W/E/A/S/D（轴控制）。需 Windows 系统。

命令行控制工具的详细说明请参阅：[tools/simple-cli/命令行控制工具说明.md](tools/simple-cli/命令行控制工具说明.md)

### Visualization

**3D matplotlib 查看器：**
```bash
cd tools/robot-viewer
python robot_viewer.py [--no-connect]
```
15Hz 查询频率下的实时 3D 可视化。使用 `--no-connect` 进行带合成关节角的离线演示。

### Simulation and Validation

**离线遥操作仿真器：**
```bash
cd tools/windows_sim
python run_sim.py [--scenario <file>] [--headless]
```
完整数字孪生，含 One Euro 滤波器、IK 求解器、安全状态机。输出 CSV 遥测数据与 JSON 报告。

**运动学验证：**
```bash
cd tools/windows_sim
python validate_kinematics.py
```
生成 1000 个随机位姿，求解 IK，验证 FK→IK→FK 往返。验收标准：位置 <0.1mm，姿态 <0.05°。

仿真工具的详细说明请参阅：[tools/windows_sim/Windows离线仿真说明.md](tools/windows_sim/Windows离线仿真说明.md)

### Firmware Build

**Keil 无头构建：**
```bash
cd robots/Dummy-Arm/firmware/stm32-control/control/MDK-ARM
build.bat
```
需 Keil MDK-ARM 安装于 `E:\KEIL\UV4`。输出构建日志，含错误/警告计数。

## 7. 风险与限制

| 风险 | 影响程度 | 证据 | 缓解措施 |
|------|--------|----------|------------|
| 无根目录设置脚本 — 用户必须手动安装依赖 | 高 — 阻塞新开发者上手 | 仓库根目录未发现 `setup.sh`、`install.bat` 或 `requirements.txt` | 创建设置自动化，含 Python 依赖安装与 OpenOCD 路径验证 |
| `build.bat` 中的硬编码路径（`E:\KEIL\UV4`）— 不可移植 | 中 — 在其他开发者机器或 CI 系统上失败 | `build.bat:1` 包含 Keil 安装的绝对路径 | 使用环境变量 `%KEIL_PATH%` 或 Windows 注册表查询 |
| `test-connection.sh` 中硬编码 OpenOCD 路径 — 用户特定 | 中 — 在 OpenOCD 安装不同的机器上失败 | `test-connection.sh` 引用 `/c/Users/TYC/.embedded-tools/openocd/xpack-openocd-0.12.0-7` | 使用 `which openocd` 或检查标准 xPack 路径（`~/.embedded-tools/openocd`） |
| `streaming_control.py` 仅支持 Windows — 使用 `ctypes.windll` | 中 — Linux/macOS 用户无法使用 50Hz 键盘控制 | 使用 `ctypes.windll.winmm`、`kernel32`、`user32` 进行高分辨率计时与聚焦检测 | 研究跨平台替代方案（`pynput`、`keyboard` 库） |
| Python 工具无 `requirements.txt` 或依赖文档 | 中 — 用户必须手动发现并安装依赖 | 未发现清单；依赖从 import 语句推断 | 从工作环境生成固定版本的 `requirements.txt` |
| 多个分散工具无统一入口点或 CLI 封装 | 低 — 开发者体验差，需记住多个脚本位置 | 工具分布于 `tools/simple-cli/`、`tools/robot-viewer/`、`tools/windows_sim/` | 创建带子命令的统一 CLI 封装 |
| 测试脚本通过路径操作引用兄弟模块 — 导入脆弱 | 低 — 目录结构变更时会中断 | `test_streaming_control.py` 使用 `sys.path.insert(0, os.path.dirname(__file__))` | 转换为带 `setup.py` 与可编辑安装的正规 Python 包 |
| `build.bat` 使用已弃用的批处理 `pause` — 在 CI/CD 环境中挂起 | 低 — 阻塞自动化构建流水线 | `pause` 命令需要用户交互 | 为非交互环境替换为退出码传递 |

## 8. 依赖关系

**Python 库：**
- `pyserial` — 串行通信（`simple-cli`、`robot-viewer`、`windows_sim` 中的所有工具）
- `numpy` — 数值计算（运动学、仿真、可视化）
- `scipy` — 科学计算（`windows_sim` IK 求解器、`validate_kinematics`）
- `matplotlib` — 3D 可视化（`robot_viewer.py`）
- `PyQt5` — GUI 框架（`chatgpt_gui.py`）
- `ursina` — 3D 引擎（`test_render.py` 诊断工具）

**系统工具：**
- `arm-none-eabi-gcc`（v12.2）— STM32 交叉编译工具链
- `OpenOCD`（v0.12.0-7）— 调试服务器（在 `test-connection.sh` 中引用）
- `Keil MDK-ARM` — `build.bat` 使用的商业 IDE（安装路径：`E:\KEIL\UV4`）
- `bash` — 执行 `test-connection.sh`（Windows 上使用 Git Bash）

**构建系统：**
- `CMake`（v4.4.2）— 28 个固件与 ROS2 包的 CMake 项目
- `colcon` — ROS2 工作空间构建工具（未安装，阻塞 `moveit2_ws` 构建）

## 9. 下一步计划

1. **创建根目录设置自动化**（优先级：高）
   - 从工作环境生成固定版本的 `requirements.txt`
   - 创建 `setup.sh`（Linux/macOS）与 `setup.bat`（Windows），实现一键依赖安装
   - 添加 OpenOCD 安装验证与路径设置

2. **添加工具专用 README 文件**（优先级：中）
   - 记录 `simple-cli`、`robot-viewer`、`windows_sim` 的用法、命令行参数与常见工作流
   - 包含常见错误（端口未找到、OpenOCD 连接失败）的故障排除章节

3. **修复硬编码路径以提高可移植性**（优先级：中）
   - `build.bat`：使用 `%KEIL_PATH%` 环境变量或 Windows 注册表查询
   - `test-connection.sh`：使用 `which openocd` 或检查标准 xPack 路径

4. **考虑统一 CLI 封装**（优先级：低）
   - 创建 `tools/robot_cli.py`，带子命令：`port-scan`、`control`、`stream`、`view`、`sim`、`validate`
   - 简化上手流程，降低新用户认知负荷

5. **研究跨平台键盘控制**（优先级：低）
   - 评估 `pynput` 或 `keyboard` 库作为 Windows 专用 `ctypes.windll` 的替代方案
   - 保持功能一致性：聚焦范围输入、高分辨率计时、双模式控制

6. **将测试脚本转换为正规 Python 包**（优先级：低）
   - 添加带可编辑安装支持的 `setup.py`
   - 消除测试文件中脆弱的 `sys.path.insert()` 技巧

7. **添加 CI 安全构建自动化**（优先级：低）
   - 将 `build.bat` 中的 `pause` 替换为退出码传递
   - 支持自动化流水线中的无头固件构建

---

相关文档链接：
- 项目总览与构建系统说明：[项目总览与构建系统说明.md](项目总览与构建系统说明.md)
- 项目进度跟踪：[项目进度跟踪.md](项目进度跟踪.md)
- 调试配置说明：[debug/VSCode调试配置说明.md](debug/VSCode调试配置说明.md)、[debug/OpenOCD调试说明.md](debug/OpenOCD调试说明.md)
- 工具说明：[tools/simple-cli/命令行控制工具说明.md](tools/simple-cli/命令行控制工具说明.md)、[tools/windows_sim/Windows离线仿真说明.md](tools/windows_sim/Windows离线仿真说明.md)
